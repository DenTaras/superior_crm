[tool.ruff]
target-version = "py313"
line-length = 120

exclude = ["alembic/versions/*"]

[tool.ruff.lint]
select = ["F", "E", "W"]
ignore = ["E402", "F401"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "e2e: E2E tests (require running server on port 8001)",
]
