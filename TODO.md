# TODO — Superior CRM

> Актуальное состояние на 16.06.2026.
> ✅ = выполнено, 🔄 = в работе, ◻ = не начато.

---

## ✅ Выполнено

- **Alembic** — миграции настроены, initial миграция создана, `env.py` использует `DATABASE_URL`.
- **PostgreSQL** — поддержка через переменную окружения `DATABASE_URL`, все ensure-функции переписаны на Inspector (не PRAGMA).
- **Рефакторинг** — `app.py` (1050+ строк) разбит на `models.py`, `database.py`, `schemas.py`, `routes/`.
- **Тесты** — 75 тестов: краевые случаи, безопасность (XSS, SQLi), производительность (тайминги), дубликаты броней, очистка заметок при удалении слотов.
- **Абонементы** — пакеты 4/8/12, списание при завершении, блокировка при 0.
- **Программа тренировки** — автосохранение (sendBeacon + debounce), перенос в журнал.
- **Журнал** — отображение с переносами строк.
- **Защита от дубликатов** бронирования на уровне приложения.

---

## ◻ P0 — Критично

- **Целостность слотов на уровне БД** — добавить `UNIQUE (slot_id, client_id)` в таблицу `bookings` (через миграцию). Сейчас защита только на уровне приложения.
- **Защита от race conditions** — транзакции / блокировки / retry на IntegrityError при создании слотов и бронировании. Сейчас проверка в приложении — уязвимо для конкурентных запросов.
- **Валидация ввода** — серверная валидация через Pydantic для POST-форм (сейчас Form-поля парсятся сырыми строками, `birth_year: int = Form(None)` выдаёт 422 на пустую строку).

---

## ◻ P1 — Высокий приоритет

- **CI/CD** — GitHub Actions: `pytest`, `flake8`/`ruff`, проверка миграций.
- **Аутентификация** — базовая auth (+ роли), если приложение будет доступно извне.
- **Пагинация** для списка клиентов (заменить `.limit(1000)` на реальную пагинацию).
- **Таймзоны** — хранить даты в UTC, конвертировать при рендеринге.
- **Flash-уведомления** — автоскрытие через JS, показывать детали конфликта (время/ID).

---

## ◻ P2 — Средний приоритет

- **Логирование** — audit log для CRUD-операций.
- **requirements pinning** — зафиксировать версии зависимостей.
- **Keyboard navigation** для календаря (стрелки, Enter).
- **Улучшить UX** модальных окон (анимация, понятные сообщения).
- **Production-конфигурация** — CORS, middleware, метрики.

---

## ◻ P3 — Низкий / Опционально

- **Уникальный constraint** `(slot_id, client_id)` в модели `Booking` + миграция для SQLite/PG.
- **Иконки / цветовая система** для статусов тренировок.
- **Деплой** — Dockerfile, docker-compose (app + postgres).
- **E2E-тесты** через Playwright.

---

## Заметки

- Проверка пересечения слотов — на уровне приложения, не защищает от гонок.
- Для продакшна: Postgres + транзакции + constraint on `bookings(slot_id, client_id)`.
- `ensure_client_columns()` и `ensure_journal_columns()` — runtime-миграции для локальной разработки; при переходе на чистый PG их можно удалить.