# TODO — Superior CRM

Это файл со сводом предложений и приоритетов по улучшению проекта.

## P0 — Критично
- Внедрить миграции (Alembic). Перенести runtime ALTER в миграции.
- Обеспечить целостность слотов на уровне БД: добавить constraint/exclude (рекомендуется PostgreSQL с EXCLUDE для диапазонов). Альтернативно — уникальные индексы или триггеры для SQLite.
- Защита от race conditions при создании слотов: транзакции / блокировки / retry на IntegrityError.

## P1 — Высокий приоритет
- Внедрить серверную валидацию через Pydantic для POST/PUT (заменить парсинг сырых форм строк).
- Показать детальную причину конфликта слота в модальном окне (время/ID конфликта).
- Добавить аутентификацию и роли, если приложение будет доступно извне.
- Реализовать пагинацию для списка клиентов (заменить .limit(1000)).
- Хранить даты/время в UTC и обрабатывать таймзоны при рендере.

## P2 — Средний приоритет
- Добавить unit/integration тесты (конфликты слотов, CRUD клиентов, бронирования).
- Настроить CI (lint, tests, flake8/black) и requirements pinning.
- Улучшить UX: автоскрытие модального flash, показать конфликтующие слоты рядом, добавить keyboard navigation для календаря.
- Логирование действий (audit log) для CRUD операций.

## P3 — Низкий / Опционально
- Перевести проект на PostgreSQL (если требуется масштабирование).
- Добавить иконки/цветовую систему для статусов, анимации модалей.
- Рефакторинг: разбить крупный `app.py` на модули (routes, models, schemas, services).

## Краткий план действий
1. Настроить Alembic и подготовить миграции — 0.5–1 дня.
2. Добавить ограничения на уровне БД (Postgres EXCLUDE) и миграции — 1–2 дня.
3. Перенести валидацию форм на Pydantic модели и вернуть понятные ошибки — 0.5–1 дня.
4. Добавить тесты для конфликтов/бронирований + CI — 1–2 дня.

## Примечания
- Текущая проверка пересечения слотов реализована в приложении; это работает для low-concurrency, но не защищает от гонок в многопоточной среде.
- Для продакшна рекомендую использовать Postgres и включить транзакции/constraints.

---
Файл сгенерирован автоматически по результатам аудита кода от агента. Если хотите, начну по пункту 1 (Alembic) или по пункту 2 (DB constraint).